<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>URL</th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do | user | %>
    <tr>
      <td><%= user.name %></td>
      <td><%= link_to user.url, user.url %></td>
    </tr>
  <% end %>
  </tbody>
</table>
